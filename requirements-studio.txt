# Sketch Studio (studio/): the prompt-to-film API and page. Optional; the core tools do not need it.
# pip install -r requirements-studio.txt
claude-agent-sdk>=0.2.159
aiohttp>=3.9
pymongo[srv]>=4.10
