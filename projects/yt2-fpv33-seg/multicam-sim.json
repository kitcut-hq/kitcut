{
  "_comment": "The round-trip test fixture for this segment. split-cameras.py conforms `source` onto a strict CFR grid as `program`, then rebuilds one synthetic camera raw per angle in `shots`. Nothing here describes the finished edit -- that is read back off the programme by shot-detect.py.",
  "_why": "Capability proof for UT-2 (@yt-2) before inviting them: 'fpv #33', a three-person Ukrainian studio podcast, 25 fps, 1080p. Chapters 2-4 of the 110-minute film (158s..1328s), which is continuous three-way conversation -- the intro montage is excluded on purpose, a montage is not a multicam cut. First stage-2 film with three distinct voices in real conversation, and the first in a language the speaker model was not trained on.",
  "id": "yt2-fpv33-seg",
  "source": "projects/yt2-fpv33-seg/sources/original.mp4",
  "program": "projects/yt2-fpv33-seg/temp/program.mp4",
  "shots": "projects/yt2-fpv33-seg/yt2-fpv33-seg.shots.json",
  "outdir": "projects/yt2-fpv33-seg/raws",
  "stagger": {
    "_comment": "Seeded so the same manifest rebuilds the same tapes. Ranges are seconds; the draw is converted to whole frames before anything uses it.",
    "seed": 20260830,
    "head_s": [1.0, 6.0],
    "tail_s": [0.5, 4.0]
  },
  "conform": {
    "_comment": "One generation, high quality: everything downstream is scored against this file, so its own loss is the noise floor of the whole test.",
    "cq": 16
  },
  "render": {
    "_comment": "The tapes. cq 16 keeps generational loss well below the structural differences the comparator is looking for.",
    "cq": 16
  },
  "verify": {
    "_comment": "freeze_db -60 is far below anything real footage registers, so a freeze found inside a live span means the graph put the wrong picture there. cover_frac is loose on purpose: freezedetect needs a moment to declare a freeze, so the first fraction of a gap never counts.",
    "freeze_db": -60,
    "min_gap_s": 1.0,
    "cover_frac": 0.75
  }
}
